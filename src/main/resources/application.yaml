spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/blogDB
    username: postgres
    password: ZimaBlue685
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  flyway:
    enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
